# Configuración de Entorno (Respaldo)
# Copiar este contenido a backend/.env si es necesario

# Servidor
PORT=3001
NODE_ENV=development

# Base de Datos
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=admin
# NOTA: Cambiar 'admin' por tu contraseña real de PostgreSQL

DB_NAME=InteractiveMapDB

# Seguridad (JWT)
JWT_SECRET=tu_clave_secreta_aqui
JWT_REFRESH_SECRET=tu_clave_secreta_refresh_aqui
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:3000
